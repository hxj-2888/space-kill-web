# 改动说明（当前版本：v32 批 4′）

> **接手入口请先读 [`docs/交接文档_v31.md`](docs/交接文档_v31.md)**（项目地图 / 红线 / 上手 SOP / 基线 / 回滚点）。
> v32 批次入口：[`docs/交接文档_推理库激活v2.md`](docs/交接文档_推理库激活v2.md)。
> 逐批细节见 [`docs/v31执行台账.md`](docs/v31执行台账.md) 与《太空杀游戏引擎.docx》第 4 章。
> v26~v29 时代的历史说明（12 份，含本文档的完整历史版本）已归档至
> `<本地>/CodeBuddy/20260910112441/_archive_docs_v26-v29/`。

## 当前基线（2026-09-10）

| 项 | 数值 |
|---|---|
| 行为指纹（200 局） | `6c04657a049dbdfdb2476dd2e0c9ae15c31338276f956232489ef7cf8b9c8bd2`（批 4′ 后逐字节不变） |
| 回归断言 | 100/100 · round-trip 0 失败 · UI 冒烟 30 局 |
| 500 局（种子 1~500） | 0 异常 · 人类 27.0% / 异形 60.4% / 外星人 12.6% · AUC(人类) .434/.410/.486 |
| 分叉度 / 硬源 | 证据层分叉度 0.3217 · 硬源锁定 2.43 条/局（私有 0.56 / 小组 1.02 / 全场 0.85） |
| 战术库 | 出口 30 条 / 6026 次 |
| 保护侧（新） | N401 773 · N403 260 · N404 2 · N405 1 · N406 979 · N407 26 |
| 通道接线 | 38/506 mounted · `chan_fired` 27 |
| 改道计数（新） | reason 487,199 · speak 9,867 · announce 3,961 · chan 1,430 · chanUniv 31,064 |

---

# v32 语言层修复：时间情境 + 角色黑话出口（用户拍板）

针对用户指出的两大痛点（「第一天就问昨晚行动」·「角色黑话没出口、语料被浪费」）：

| 修复 | 内容 |
|---|---|
| 时间情境 | `decide.speak` 质询分支与 `renderer` ask case 增加第 0 夜（开局讨论）专属模板——开局没有「昨晚」可问；`Speakable` 增 night0 守卫（私有体验宣称在第 0 天全部失格） |
| 黑话出口 ① | 保镖（R04）：保护对象公开背书 `defend`（「我保 X」，parser defendT 已识别，round-trip 过） |
| 黑话出口 ② | 验票官（R07）：票源观察转述 `quote`（N219 的正主出口——此前票型转述规则登记了却无生产者） |
| 现有出口确认 | 抑制/维修/制药体验（Speakable 原有三类）· 神探查验汇报 · 船员二查 · 警长/武装亮身份 · 异形战术库 37 条 |

**验收**：round-trip 97/0（开局模板入校验）· 回归 100/100 · 500 局 0 异常 · 人类 31.4% · 指纹 `d6da46ba…`。
**局内口径分叉度首读 0.5057**（全局聚合 0.314——私有流水跨局聚合稀释效应被证实，新口径作为 5′+ 基线）。

---

# v32 前端公平性改造 + 核心原则入档（用户拍板）

## ★ 核心原则（正式入档，后续所有批次必须遵守）

> **人类玩家参与比赛，除了不把他的推理链以及个人思考信息录入系统之外、不把玩家单独对 AI 的
> 怀疑度计算在内之外，其他一律平等。不得再把通道单开给 AI 而不开给真人。真人除了不开上帝视角、
> 不透视 AI 信息之外，AI 有的机制人类都该有。**
> （推论：isHuman 只能表达「决策来源」与「上帝视角渲染」，不得 gate 任何机制的适用性；
> 违反点由 assertHumanAiParity 断言在回归中拦截。）

## 前端改动

| # | 内容 |
|---|---|
| 1 | **怀疑度全面撤出默认 UI**：玩家名单的「怀疑 N」标签、阵营页签的威胁度榜——全部改为仅 DEV 可见（此前非 DEV 下 heat≥40 就在名单上挂标签，属默认 UI 泄露） |
| 2 | **账本并入 DEV**：删除顶部「账本」按钮与独立覆盖页，账本内容作为 DEV 总览的「我的账本」区块渲染（只有开 DEV 才能看） |
| 3 | **开始页底部三键上移为顶部按钮**（用户纠正：三键指开始页底部的「音乐音效 / 规则速览 / 黑话术语表」，非游戏内按钮）：开始页顶部新增三按钮——「♪ 音效：开/关」（toggle，状态真源为隐藏的 #bgm checkbox，开局逻辑零改动）、「📖 规则速览」（新独立覆盖页 #rules-overlay）、「💬 黑话术语表」（slang-overlay 上移到全局层级，开始页/游戏页均可打开）。原页底两个 details 折叠与 checkbox 删除 |
| 4 | **游戏内交互键保持原位**（用户纠正：未要求改动游戏内按钮布局——全屏行动面板方案已整体撤销，renderStage 恢复原版） |

## 联机多真人就绪度评估（用户问「两个/三个/全是真人呢」）

**引擎层：无单真人阻塞假设**——`g.humans` 数组、beginStep 对每个真人席位独立生成表单
（`humSet` 多成员）、真人↔真人私聊配对路径无 isHuman 假设、机制对等后每人各自持有
tEvents/known/cred（per-player，天然多席位）、toDecision 探测器对多真人同样生效。
**待办（UI/联机层）**：ui.js 以 `g.humanId` 渲染「我」的视角（单机假设），联机版需按客户端
会话映射「当前视角席位」（net.js 已有载荷雏形）；`g.dev` 联机载荷结构已双轨（dev.skills）。
结论：多真人不改变引擎语义，主要是 UI 视角映射工作量。

---

# v32 isHuman 语义收窄（用户拍板「把 isHuman 关进笼子」）

**采纳用户的架构判断**：骨架不动（决策来源可插拔、规则层无感是对的），做语义收窄——
`isHuman` 从「规则适用性开关」降级为「决策来源标记」，只允许出现在 engine.beginStep（给表单
还是调 AI.decide）与 ui.js（渲染什么）+ 少数带注释的合法点（显示口径/生成侧过滤）。

## 落地四条

| # | 内容 |
|---|---|
| 1 禁区 | 规则层（belief/perceive/decide/moe/channels.run/pipeline/registry）非法读取清零：moe 的 tiedClaims/queueVerify 不再 gate 真人（数据同构挂着无害）；白名单仅存 consensus 显示口径、异形队友自动营救、真人应答转交、生成侧 Z 过滤四处（全部带注释说明语义） |
| 2 空壳同构 | 真人 tEvents/uEvents/hardFloor/cred 与 AI 同构（上轮已放开，本轮入断言） |
| 3 探测器 | `toDecision` default 从静默 `return {}` 改为 **throw**——**立刻炸出一处真实歧视**：`kind='speech'`（玩家验票官的 30 秒专属发言）从未登记，提交的正文恒被吞成 `{}`。已补登记 |
| 4 断言 | `assertHumanAiParity` 三重断言进回归（103 条）：① 规则层源码扫描零非法 isHuman（跳过注释，白名单带注释）② 全部决策 kind 必须登记 toDecision ③ 真人席位机制链路对等（onAccuse/addUniversal/setHardFloor 对玩家真实入账） |

**黑话进 AI 嘴**（用户拍板「黑话没在玩家视角出现」）：指控/质询/做戏模板注入行话措辞
（sus / 划水 / 盘 / 踩 / 聊爆 / 带节奏 / 摊牌）——全部是 NLP 已支持的行话，玩家发言记录里
可核对「原意」解析。

**关于 0c 私聊的「第 13 处」**：`pa.isHuman ? null : AI.speak` 属决策来源 ①（真人留空 = 不发言，
AI 留空才由系统兜底）——合法保留；且玩家接收方机制已对等（known/证据入账）。

**验收**：回归 **103/103**（+3 对等断言）· round-trip 98/0 · UI 冒烟 30 局 0 异常 ·
500 局 0 异常（人类 31.4%）· 指纹 `b14d8ee5…`（黑话模板改变发言文本，预期；isHuman 收窄部分
不动 AI 路径）。

---

# v32 机制对等·二轮：歧视点清查 + 私聊/阵营频道修复 + 专家增设（用户拍板）

**设计原则（用户原话）**：除了记忆和推理能力（AI 的账本积累是 AI 的能力优势），其他机制玩家和 AI 平等。

## 一、歧视点清查与修复（端到端实测驱动）

| # | 歧视点 | 修复 |
|---|---|---|
| 1 | **玩家改口跳身份被静默吞掉**：说过一句「我查过 X」→ 被 onReport 隐式自证钉为船员 → 之后跳神探/医生 `!p.claimedRole` 直接跳过（AI 有 decide 分支不受此限）。端到端实测：「我是神探」0 条入账 | `pipeline claimRole` 允许改口（R38 照记 + R12/R30 对账兜底，改口有代价不被吞）。修复后 14 条入账 ✓ |
| 2 | **玩家私聊接收零机制落点**：`steps 0c` 只在接收方是 AI 时调 onPrivate——玩家收到私聊只看得到文本，known 弱记录/证据台账不更新 | 移除 `!pb.isHuman` 条件（onPrivate 内部守卫已放开） |
| 3 | 玩家指控/查杀/质询 | 端到端实测已全通：指控 41 条 / 查杀 27 条（locksay+accuse+grudge 三链）/ 质询 26 条 ✓ |

## 二、异形白天阵营讨论栏（此前完全没做）

- **UI**：讨论窗口新增「队内频道」输入行（仅异形玩家可见）——白天密谈即时投递队友 inbox +
  `privateSay`（D 档入账 AI 队友）；「阵营」页签显示密谈记录。
- **引擎**：白天讨论收尾时 AI 异形 22% 概率产队内密语（quiet 语料）→ 队友 inbox + D 档入账——
  白天不再是只有夜间 0c 一次队内机会。

## 三、账本去 DEV 化

按钮 class 移除 `dev` 依赖——账本对**所有玩家**生效（不依赖 DEV 开关）；页内数据全部来自玩家
席位自己的真实机制资产（上轮机制对等化的产出）。

## 四、语言库激活

- **N336~N348 安全话术池放开人类 AI**——黑话不再只属于坏人阵营，好人 AI 的垫场话不再是干巴巴的中性模板。
- 黑话出口现状：神探查验/船员二查/警长亮身份/保镖背书/验票官票源转述/体验宣称/安全话术/异形战术库 37 条。

## 五、专家增设（批 7′ 前置落地）

- `registry.js` 新增 **G1~G8 通用专家注册表**（方案 §4.2 的 E 组合并映射）：G1 时序←E9+E7 ·
  G2 社会网络←E11+E3 · G3 生死←E6+E4 · G4 物理←E1+E5 · G5 感染←E2 · G6 身份锚定←E8+E10 ·
  G7 反事实←E13 · **G8 元认知=空席**（0 条资产，资产定义待拍板 §9.3）。
- R01~R13 角色专家（批 5′）：挂载 + ATTEND 调制 + 私有流水已生效；`GATE_IMPL` 通道执行器即
  「专家产出 Claim」的运行形态。
- **通道接线 38/506 不变**——506 条二维绑定是批 7′ 主体工程（每条需独立 gate 实现），本轮未动，
  如实记录；接线序见交接文档 §6。

**验收**：回归 100/100 · round-trip 98/0 · UI 冒烟 30 局 · 500 局 0 异常（人类 31.6%）· ③ 公告 238 次。

---

# v32 机制对等：玩家席位全面接入游戏机制（用户拍板「玩家在时机制不生效、人机占位生效」）

**根因**：全仓 ~30 处 `isHuman` 守卫把玩家席位排除在证据机制之外——AI 一进局就在记账，
玩家一进局就是「只能看公告的旁观者」。涉及：

| 层 | 修复前玩家拿不到的资产 | 修复 |
|---|---|---|
| `belief.js` | tEvents 证据台账 · 硬源置位（R30 假冒/R7 假标记）· 可信度 C 表 · 普适层 uEvents | 四处入口移除 isHuman 排除 |
| `perceive.js` | 全部 R 规则链入账（R38 自证/指控/追责/对证/票型 R27~R40/king 指纹/质询应答/私聊） | ~30 处观察者循环放开 |
| `pipeline.js`/`announce.js` | 查验汇报目标侧证据 · 体验宣称 · 证伪对账 · shareSay | 放开 |
| `channels.run.js` | 全部已接线通道的证据（N401~N407 等私有通道读玩家真实私有字段） | 放开 |
| `moe.js` | 统一入账口/私有源流水/路由仲裁对玩家 | 放开（tiedClaims/queueVerify 保持 AI 专属） |
| `steps.js` | **玩家医生的 markSeen/markEverSeen 标记记忆、玩家的 dyingSeen 濒死名单** | 放开 |
| `engine.js` | 玩家异形的标记清单流水 | 放开 |

**保留的玩家专属分支**（应然设计）：pendingAsk 真人应答 · 战术库生成前过滤只对 AI 发言 ·
updatePublicThreat 共识口径 · 异形队友自动营救。

**新增「账本」页**（顶部按钮，全屏覆盖页，Esc/✕ 关闭）：玩家的怀疑度三阵营分布（与 AI 投影同源）、
危险度、硬源清单、证据台账（最近 30 条）、角色资产卡（神探查验池/船员二查/医生标记记忆/
救援濒死名单/警长子弹流水）。**与 AI 同一份机制产生的数据，不是摆设**。

**硬锁机制补完**（用户拍板「公告/暴露一律硬锁，危险度拉满」）：盘点确认 ③④⑤⑥⑩ 全部已走
`revealPublic` 硬锁（knownLockOf 唯一路径）；补 `dangerOf` 钳制——人类观察者对官方确证的
**异形危险度 = 100、外星人 = 90**（「这个人不用再算了」）；确证己方走既有 hostile≤0 归零。

**验收**：回归 100/100 · round-trip 98/0 · UI 冒烟 30 局 · 500 局 0 异常（人类 31.2%）·
清单校验 ✓ · 实测玩家席位 5 夜后 tEvents 13 条（AI 席位均值 10，同量级）· known 8 条 · uEvents 8 条。

**待办（前端体验，用户已提出）**：顶部按钮防突变（banner 改浮层 / hud 固定高）、
主页底部交互键上移顶部按钮化 + 独立页渲染。

---

# v32 修复：③ 公告利用率（用户拍板「公告了也不拉满、晚上也不攻击」）

**根因链（三处叠加）**：
1. **AI 神探几乎从不走官方 ③ 公告**——`uPub` 公式对公告**扣减**敌方存活数 ×10（方向反了：
   敌方活着越多公告越紧迫），实测 uPub ≈ 20 < uCheck ≈ 32 ⇒ 神探只查验 + 白天口头汇报；
   口头汇报按 v31 裁定**不写硬锁**（可伪造宣称）⇒ 全场只拿 B−/C− 弱证据。
2. 于是「怀疑度不拉满、警长不开枪（U=1.4×Dg−1.4×(θ+35) 过不了线）」——**不是硬锁链路坏了**：
   `revealPublic → p.known → knownLockOf → project 硬锁 p_alien=1` 一直是好的，是公告根本没发生。
3. UI：神探「发布公告」切到已查验池页签后，池为空时无任何提示（体感「没有可选项」）。

**修复**：
| 处 | 内容 |
|---|---|
| `decide.js` | `uPub = 查杀60 + 敌方存活×10×(θ/50)`（正向紧迫度，θ 调制保留）——公告查杀优先于继续查验；金水 −30 不变（防冒领博弈） |
| `ui.js` | 神探表单池为空时显式提示「先查验，再公告」 |
| `tools/mc.cjs` | 新增仪器：官方 ③ 公告次数（公告先得发生才有用） |

**验收**：回归 100/100 · 500 局 0 异常 · **③ 公告 232 次/500 局**（修复前 ≈0）· 人类 31.6%。
公告后的硬锁行为链（怀疑度 p_alien=1 拉满 → 投票聚焦 → 警长开枪 U 过线）由既有硬源断言覆盖。

**立卡（批 7′ 前置，回答「推理库打通了吗」）**：
1. **通用专家 G1~G8 未做**——E1~E13 全部是主题专家且无 `run` handler（方案 §4.2 映射：时序一致性←E9+E7 · 社会网络←E11+E3 · 生死账本←E6+E4 · 物理资源←E1+E5 · 感染流←E2 · 身份锚定←E8+E10 · 反事实←E13 · 元认知←E12 空席）。
2. **角色专家 R01~R13 已挂载未上工**——批 5′ 完成 `p.roleExpert` 挂载 + ATTEND 调制 + 私有流水落盘，但无 `run` 产出 Claim（「挂了名、没上工」）。
3. **通道接线 38/506**（7.5%）——批 7′ 二维绑定是主工程，接线序见交接文档 §6（E8 38 条 → E13 74 → E10 81 → ⑥ 20 → E3 47 → E11 37）。
4. 仲裁差一档/差二档恒 0（批 8′ 才打开）。

---

# v32 批 4′：统一入账口（emit 拆 absorb + arbitrate 直通）

依据：《太空杀·注意力与记忆优化方案 v1.0》六章批 4′ ＋ [`docs/交接文档_推理库激活v2.md`](docs/交接文档_推理库激活v2.md) §6
（**验收口径：指纹逐字节不变（变了 = 仲裁动了写入）；chan 求值/命中计数应变化；新增「各路径改道计数」仪器**）。

## 一、改了什么

| 项 | 内容 |
|---|---|
| `moe.js` 新增 `absorb` | 四类写入路径的统一入口：改道计数 → （可选）route 求值 → `arbitrate` 直通写入 |
| `moe.js` 新增 `arbitrate` 直通模式 | 逐条写入、**不裁剪、不饱和、fold 关闭、queueVerify 不消费**（裁剪推迟到批 8′） |
| `moe.js` 新增 `writeRaw` | 直通写入 = 调用点原值逐字段透传（delta/tier/src/grudge/kind/speakerId/chan/expert），**不走 write() 的 SCORE 重算** |
| `moe.js` 新增 `absorbUniversal` | N406 等普适层收口（`addUniversal` 不走 tEvents，无仲裁语义 → 只计数 + 透传） |
| `perceive.js` | 路径②（R 规则链 ~25 处 addEvent）经原别名一次改道 `MoE.absorb(path='reason')`，调用点零改动 |
| `pipeline.js` | 路径①（locksay / excludesay / exp / denyLie 4 处）→ `path='speak'` |
| `announce.js` + `steps.js` | 路径③（shareSay / crewExclude）→ `path='announce'` |
| `channels.run.js` | 路径④（通道写入）→ `path='chan'` **并接入 route 求值**（`opts.route=true, evt=ch.id`）；普适层 → `absorbUniversal` |
| `tools/mc.cjs` | 新增 `B1_改道` 指标组 + 控制台读数 |

## 二、验收读数（全过）

| 项 | 结果 |
|---|---|
| 回归断言 | **100/100** |
| 指纹（200 局） | `6c04657a…` **逐字节一致** |
| 500 局 | 0 异常 · 人类 27.0% / 异形 60.4% / 外星人 12.6% · AUC .434/.410/.486 · 分叉度 0.236/0.3217 —— 全部与基线一致 |
| UI 冒烟 | 30 局无异常 |
| 改道计数 | 五条路径全部 > 0（见基线表） |
| chan 求值/命中 | 通道事件首次参与 MoE route 求值（`stats.byEvt` 新增 chan 维度） |

## 三、诚实记录与批 8′ 待办

1. **直通模式的幅度对账未做**：部分调用点 delta 不是裸 `SCORE[tier]`（R38 自证 `−round((v−bias)×half)`、
   R31 记仇 `min(base, 25−sum)`、私聊 `×(0.4+0.6C)`、promiseMiss `PV.miss` 表、onExpose 30/32 阶段系数）——
   全部以原值透传保持指纹不变；收编进 `mult` 或改档位定义，留到批 8′ 打开裁剪时逐条对账。
2. **route 求值只对通道路径开启**：其余路径的 route 评估在 4′ 会把「未激活专家」的 Claim 也写进去
   ——与 emit 的过滤语义冲突；按 5′（角色注意力定激活维度）→ 7′（二维绑定）的顺序再开。
3. `setHardFloor`（R30/R7 置位）与 `p.known` 硬源本来就不走 addEvent，**不在改道范围**（交接文档对账点 1 例外项）。
4. `MoE.stats` 重合度统计因通道事件入 route 而基线漂移（仪器读数，预期内；分叉度指标不读它，不受影响）。

---

# v31 批 3.5：人类侧保护专项（N401~N407 ＋ N414~N417）

依据：《太空杀·人类侧保护专项（增补第二十二章）》（推断通道 N401~N407 ＋ 行为选项池 N414~N417）。
**保护侧首轮开垦** —— 总表 v20 几乎全是「识破知识」，浑水摸鱼 v21 是「异形/外星人能说什么」；
**保镖保护谁 / 警长巡逻谁 / 医生先治谁，此前无人管**（三个分支的目标选择只有通用 `dangerOf`）。

## 一、7 条保护侧通道（一条通则 + 三条硬规）

| 编号 | 强度 | 落地形态 |
|---|---|---|
| N401 | C+ | ④ 暴露 ⇒ 必为工程师系且必为人类。**④ 暴露已是硬源**（红线 5），增量在定案 1 的缺口：④ 公告补标原职业 ⇒ 泄露「转职已发生 ⇒ 存活≤6 或已过第 6 夜」 |
| N402 | C+ | 巡逻一次性。**不单独入账**（不指向个体、方向中性）→ 以公共前提 `MoE.patrolSpentPublic(g)` 交付 |
| N403 | C+ | 第 4 夜起单次被挡 ⇒ 层收敛（排除巡逻层）⇒ 轻微指向非人类 |
| N404 | C+ | 连续两夜被挡 ⇒ 非人类（**口径纠正**：N306 写「异形」，本条只到「非人类」） |
| N405 | C | 被挡 ≥3 个**非连续**夜次 ⇒ 存续型抵挡层指纹 |
| N406 | B+ | 濒死名单 − ⑥ 死亡名单 = 被救回者 ⇒ 实际攻击次数（**证群体不证个体 ⇒ 普适层**） |
| N407 | B | 抗体生效 × 目标存续（三方对账）⇒ 该夜确有一次感染施加于 X，X 其后无标记落地 |

**核心裁定（文档第四章，上升为通则）**：凡由私有攻击反馈推出的目标身份推断，**一律不进普适层**
—— N403/N404/N405 全部 `universal:false`（回归断言锁死），只写该攻击方对目标的敌对度。
**N403/N404/N405 触发面互斥**：`once`（仅 1 夜）→ N403 · `consec` → N404 · `persist` → N405。

## 二、4 条保护优先级（行为选项池，**不进 channels.data**）

| 编号 | 旧 → 新 |
|---|---|
| N414 保镖 | `if (exposed) return exposed[0]` **硬规则** → `U = −dangerOf + protectionBonus` 效用权重 |
| N415 警长 | 「自己 + 已暴露工程师 + dangerOf 最低 1 人」硬插队 → 同权重的连续效用（含「不可替换性」） |
| N416 医生 | 治疗候选仅「感染者」→ 扩为 `感染者 ∪ 关键预告者`（**预投抗体**；治疗即赋抗体，`had=false` 照样授予） |
| N417 下界 | 新增 `argmaxProtect`：`ε ≥ 5%` 且**硬源确证（conf=1）也不归零**（否则保护优先级退化成硬规则） |

权重真源 = `Tiers.PROTECT`（可标定）；优先级阶梯 `Tactics.protectPriority`：3 已预告/④ 暴露 ＞ 2 已确证人类的关键职位 ＞ 1 高价值未确证 ＞ 0。

## 三、引擎侧三处落盘缺口

| 改动 | 位置 | 原因 |
|---|---|---|
| ④ 暴露公告补标原职业 | `steps.js '4a'` | 定案 1 在 ④ 一直漏（⑥⑩ 早已标）⇒ N401 连带无输入 |
| `p.dyingSeen`（濒死名单） | `steps.js '8'`（救援结算**之前**） | P13 此前 AI 侧零落点（总表自评「本轮最大漏记」）⇒ N406 无法变现 |
| `p.antibodyFired`（抗体生效） | `engine.js applyInfection` | P15 此前只有 `priv()` 文本 ⇒ N407 无素材 |

## 四、验收与诚实记录

| 项 | 结果 |
|---|---|
| 回归断言 | **100/100**（71 → +29） |
| round-trip / UI 冒烟 | 0 失败（96 条）/ 30 局无异常 |
| 500 局 | **0 异常** · 人类 **27.0%** / 异形 **60.4%** / 外星人 **12.6%** · AUC(人类) .434/.410/.486 |
| 通道接线 | **32 → 38 / 506** |
| 指纹 | `6c04657a049dbdfdb2476dd2e0c9ae15c31338276f956232489ef7cf8b9c8bd2` |

**诚实记录**：① 人类 −0.2pp（噪声内，但 N416 的「3 次额度反复预投同一人」与 N403~N405 对
「异形刀人类」也会写非人类证据这两点方向上都可能压人类 → 按红线 4 **不用胜率反推权重**，
留待文档 8.2 的单体层增量标定）；② N404/N405 出口极少（2/1 次，判据本就稀有）；
③ **N407 的结论方向被修正为「信任」**（本引擎抗体先于效果免疫结算，原文「⇒ X 为异形」不可判定）——**需拍板**；
④ N402 无独立出口（前提型通道的必然结果）。

---

# v31 批 3：战术库 37 条接线 + 三项裁定

依据：方案 §8.1 批 3（「战术库 37 条接线」，方案称为**平衡的真实杠杆**）+ 用户对三项待裁定的拍板。

## 一、战术库从死数据变成可执行选项池

`js/corpus/tactics.js` 的 37 条（N349~N386）此前是**纯字符串数组、全仓零引用点** —— 异形/外星人的全部专属手段一件没接（不会冒领、不会卖队友、不会欺诈感染、不会分工）。这不是「战术弱」，而是工具箱是空文件，是台账 D1（AUC 敌友倒置）与 D2（AI 同质化）的直接根因。

现改造为结构化选项池（`id / risk / when / say / act / note`）+ 唯一消费者入口 `Tactics.pickTactic`，由 `decide.speak()` 的非人类分支消费，Claim 仍过 `filterClaims`（Z 档禁区在**生成前**拦截）。认知类条目（N361/N369/N370/N385…）保留 `note`、`act = null` —— 它们的语义是「什么时候别做什么」，接在门禁与调度上。

## 二、500 局出口读数（验收口径达成）

| 战术 | 出口次数 |
|---|---|
| N357 冒领船员 | **344** |
| N360 欺诈感染 | **219** |
| N372 三只分工：伪装/破坏/潜伏 | **59** |
| N359 牺牲队友 | **47** |
| N382 外星人冒领船员（不受公告清零约束） | **34** |
| N358 卖队友 | **27** |
| 合计 | **30 条 / 5896 次** |

**方法论修正**：卖队友/牺牲队友的门禁首版读「己方怀疑度」，实测恒 0。探针（60 局 / 21136 次异形发言）定位根因：**异形对自己队友的怀疑恒为 0**（认知层把同族标 alienMate，hostile 只对他族计数）→ 门禁结构上不可达。改为读**公开可见的队友处境**（被指控 ≥2 次 / 已公开暴露 / 已被揭示）后可达。

## 三、三项裁定落地

| 裁定 | 落地 | 读数 |
|---|---|---|
| **口头汇报 ≠ ③ 官方公告** | 神探/船员的口头查验汇报不再写全场硬锁（`p.known`），只按可伪造的宣称入账（目标侧 `locksay` + 说话者自证）；硬锁只留给官方 ③ 公告与角色私有查验 | 全场硬锁 0.9 → **0.7** 条/局 · 单人私有 0.52 → **0.59** · 证据层分叉度 0.3173 → **0.3239** |
| **AI 加「否认」话术** | `decide.speak()` 被指控时产 `deny`（about=checked / infection）——可被观察者私有知识当场证伪（`denyLie` → A⁻） | 证伪型对账 **0 → 86**（A2 的根因是生成端 0 处） |
| **自证「调档位」** | `Tiers.SELF_CLAIM` 删除 `crewBias` 特例（旧实现 crew 强度被调两次：档位一次、bias 一次）；只剩档位一个旋钮 | crew 自证 −8 → **−1**（有效 −5.6 → ≈ −0.7） |

## 四、验收

| 项 | 结果 |
|---|---|
| 回归断言 | **71/71**（64 → +7：战术库结构 / N357 动作 / N400 生成前拦截 / N372 分工 / 口头汇报不写硬锁 / 口头汇报仍入账 / AI 否认） |
| round-trip / UI 冒烟 | 0 失败（断言数 94~96，随随机语料浮动）/ 30 局无异常 |
| 500 局 | **0 异常** · 人类 27.2% / 异形 59.6% / 外星人 13.2% · AUC(人类) .432/.412/.477 |
| 指纹 | `4fa952e76e1ccc68f5c0967e62f191fd789c4c1011e681f2181d417e74ca2826` |

---

# v31 批 0~2（摘要，详见 docs/v31执行台账.md）

| 批 | 内容 | 验收 / 读数 |
|---|---|---|
| 0 清理 | 删 `threatOf` 别名（名实不符：名为威胁度、实返怀疑度）→ 3 处 DEV 调用点改 `suspOf`；文案与注释口径修正 | 行为中性：指纹逐字节一致 |
| 1a B4+B2′ | `tierFor / magFor` 视角依赖档位能力（write / arbitrate / channels.run 三处生效）· `SCORE['P'] = 10` + `TIER_BY_VAL` 排除 P | 行为中性：指纹不变 · 总表视角注解 15 条已普查 |
| 1b N216 | 视角档真正生效：该门禁只对验票官触发 ⇒ 恒 A−（此前按单值 D 写，低估 6.6 倍） | AUC 残局 0.494 → 0.498 |
| 2 A3 | `Tiers.PROMISE` 词表统一 · 真神探指名预告（`kind=announce`，承诺夜 +2 按「是否发布该目标公告」结算）· 承诺者/目标出局 ⇒ 失效不判违约；**连带修两处**：A01/A02 方向写反（漏 `neg` ⇒ 写成 +A+ 可疑）、`uCheck` 为 const 导致 `+= 80` 抛异常（72/500 局崩） | **A01 命中 0 → 160** |

**诚实记录（批 2 遗留）**：预告机制当前让人类 **−4.7pp**（300 局同种子：机制关闭 34.7% → 开启 30.0% → 加强制履行 27.0%）。原因是神探公开预告＝暴露身份、异形优先刀他（57 次预告 56 次神探先死），而**暴露代价尚未进入发言决策的效用**。下一批正面对攻项（与 `exposeRiskInc` 同族），**不建议靠调低预告频率找平**。

---

# 未决项（需拍板 / 待做）

完整清单见 [`docs/交接文档_v31.md`](docs/交接文档_v31.md) §4.2。要点：

1. **B1 破坏路径**：`uDestroy = pressure×100`（cd 大时趋 0）与 `uAct = threatTop×0.9` 量纲不对等 → 破坏系统近似摆设，需给破坏一个与刀口可比的效用基线。
2. **B3 exclusion 方向 + 量级**：贝叶斯上轻指非人类（+4.7pt）与「查验清人」语用相反；最小档 D− 会产生 +12~18pp 偏移 → 方向与量级必须一起定。
3. **B7 `SHADOW_FACTOR` 标定**：前置已就绪（分布可读 p50 −0.0120 / p90 0.0168 / max 0.2946）。
4. **B2 P 档 41 条**：已显式映射为 10，但 41 条私有源条目仍未接线。
5. **A02 匿名预告**：只产指名预告（A01）。
6. **认知类战术条目**（N361/N369/N370/N385）是否需显式门禁。
7. **N407 结论方向**（批 3.5）：总表原文「⇒ X 为异形」，但本引擎抗体先于效果免疫结算 ⇒ 不可判定；
   已落为【信任】方向，需拍板是否接受。
8. **N403~N405 幅度**（批 3.5）：层收敛后「保镖保护人类」仍是常见解释 ⇒ 存在假阳性（人类 −0.2pp）；
   需按专项文档 8.2 做单体层增量标定，并决定是否纳入人类攻击方（警长/武装）。
9. **N416 抗体存续口径**（批 3.5）：抗体只持续 1 夜 ⇒ 「优先给预告者」等于多夜反复预投，
   异形不选感染方向时是纯损耗；是否改为「存续到被消耗」（规则层变更）。
10. ~~性格危险度基线标定~~【**2026-09-10 已整体回退**】：曾把 `BASE_DANGER` 30/50/70 → 40/55/70
    （该常量零读取点，行为中性），随后按 k={25:4/3, 50:1.1, 75:1} 等比放大 6 项危险度同轴分档值
    （actLine / voteParam / abstain / swapOff / vSelf / thetaShift）⇒ 实测 **人类 27.0% → 25.4%、
    异形 60.4% → 61.6%** ⇒ 用户裁定**整体回退**（基线一并回退）。
    现全部为原值：回归 100/100 · 指纹 `6c04657a…`（与批 3.5 逐字节一致）· 500 局 27.0/60.4/12.6。
    保留结论：`BASE_DANGER` 仅登记不生效；真正生效的是 10 个 θ 旋钮，下次调性格请**逐旋钮 A/B**。

# 回滚点

`_backup_v31b35/js/` · `_baseline_fingerprint_200_v31b35.json`（`6c04657a…`）· `_mc_result_v31b35.json`
（均在 `<本地>/CodeBuddy/20260910085834/`；更早各批次存档同目录）。

# 下一批（**已由《推理库激活方案 v2》接管**）

原批 4~7 取消，改为 **批 4′~8′**：4′ 统一入账口（emit 拆 absorb+arbitrate，指纹必须逐字节不变）→
5′ 角色注意力（13~16 个，含转职系）→ 6′ 拟人化记忆（关注名单 3~5 人 + 显著性连续权重 + 近因/首因遗忘）→
7′ 通道接线 506 条二维绑定 → 8′ 仲裁器激活（差一档/差二档首次 >0）。
**接手入口：[`docs/交接文档_推理库激活v2.md`](docs/交接文档_推理库激活v2.md)**（方案 → 引擎落点的逐条映射 + 已拍板项 + 四条红线 + 待拍板残留）。
配套：`docs/审查_推理链与语言库利用率_2026-09-10.md`（506/38/23 的利用率普查与缺口清单）。
