# 推理链与语言库利用率 · 全面审查（2026-09-10）

对照材料：《太空杀规则正式版 v4.1》·《推断通道与噪声·总表（合并版 v20）》·《浑水摸鱼系统·战术库（v21）》·《人类侧保护专项（增补第二十二章）》
审查对象：`C:/Users/ASUS/Desktop/太空杀游戏-完整版`（v31 批 3.5 · 指纹 `6c04657a…`）
数据来源：500 局蒙特卡洛（种子 1~500，0 异常）+ 全仓逐文件静态普查（`js/` 29 文件 + `tools/` + `server/` + `index.html`）

---

## 0. 结论速览

| # | 结论 | 数据 |
|---|---|---|
| 1 | **资产没有丢**：三份资产文档与代码库完全对账 | v20 文档 500 编号 − 已撤回 P11 + 第 22 章 7 条 = 代码 506 条；v21 的 N336~N400 共 65 条全部收录 |
| 2 | **推理库利用率 7.5%**，且触发率更低 | 506 条 → 接线 38 条 → 500 局内**触发过 23 条**；另有 **15 条注册了但一次没命中** |
| 3 | **激活链条有结构性断点**：MoE 只挂了 3 个事件 | `MoE.emit` 仅 ④/⑤/reveal 三处；其余证据写入**全部绕过 MoE** 直写 `addEvent/addUniversal` |
| 4 | **13 个专家是「标签」不是「模块」** | `EXPERTS` 表无任何 `run` handler；E7/E9/E10/E13 四个标签无打标者（不可达）；**E4 查验分析、E12 元认知在总表里 0 条资产** |
| 5 | **语言库 22 种 kind 只用了 12 种** | 生产端只产 12 种；`defend/rally/split/quote/vote/bind/pass/destroy/cure/rescue` 10 种**没有 AI 生成端**（只有 NLP 识别与 pipeline 消费，全靠玩家说出） |
| 6 | **语言库在 3 个规则环节对 AI 完全闲置** | 遗言（2.6.1）· 验票官专属发言（4.9）· 身份备注（2.7）：三个 kind 的 `req` 都带 `p.isHuman` 过滤 / 字段零消费 |
| 7 | **死规则 1 条、死常量 1 个、死导出 20+ 项** | `RULE.voteQuote` 零读取；`Tiers.BASE_DANGER` 零读取；`AI.threatOf` 注释称已删实际还在；`IR.validate` 定义了但零调用（kind 白名单不生效） |
| 8 | **两个工具入口的加载清单已失效** | `tools/verify-heat.js` 与 `server/server.js` 未按 `load-order.cjs` 更新，按当前源码会因缺模块直接加载失败 |

**一句话**：资产是齐的，问题是「接得少 + 激活窄 + 语言库单向」。推理库 94% 的条目、语言库 45% 的 kind、专家表 4 个席位、规则 v4.1 的约 10 个信息机制，目前都处于「登记了但没通电」状态。

---

## 1. 资产对账（文档 ↔ 代码）

| 资产文档 | 文档内编号 | 代码库 | 差异 |
|---|---|---|---|
| 总表 v20 | 500 个唯一编号 | `channels.data.js` 506 条 | ① 文档有、代码无：**P11** —— 但 v20 原文已自注「原 P11（蛰伏泄露外星人存在）已撤回：外星人在场是纯公开事实」，**属文档内已作废条目，非丢失**；② 代码有、v20 无：N401~N407（第 22 章增补，另有专项文档），**符合预期** |
| 战术库 v21 | N336~N400 共 65 条（另引 N143/N145/N159/N211/N221/N239/N253/N260/N262/N281/N282/N301/N324/N325/N332/N335） | `tactics.js` SAFE 13 + ALIEN/XENO 37 + PHASES 7 + 排错 4 | **65 条全部收录** ✓ |
| 保护专项 第 22 章 | N401~N407 + N414~N417 | 通道 7 条全部入总表；N414~N416 在 `tactics.js`、N417 在 `Tiers.PROTECT` | **按文档 5.4 归属落地** ✓（N414~N417 不进总表是文档明文） |

> 结论：**「资产丢失」一类问题不存在**。审查焦点应全部放在「利用率」与「激活链」上。

### 总表 506 条的静态分布

| 维度 | 分布 |
|---|---|
| 档位 | C+ 129 · C 108 · C− 88 · **P 46** · Z 26 · A− 18 · D− 14 · D+ 13 · B+ 13 · B 10 · A 7 · D 10 · B− 7 · F 16 · A+ 1 |
| 事件 `on` | ⑤ 122 · * 112 · infect 89 · vote 67 · claim 33 · ④ 32 · check 22 · ⑥ 20 · pm 8 · ③ 1（**①/②/⑦/⑧/⑨/⑩/⑫/⑥以外的公开批次没有专属事件键**） |
| 专家归组 | E1 130 · E10 81 · E13 74 · E3 50 · E11 43 · E2 41 · E8 38 · E9 25 · E7 21 · E5 2 · E6 1 · **E4 0 · E12 0** |
| `dormant` 字段 | true 500 / false 6 —— **字段已失真**：其中 32 条实际已接线却仍标 `dormant:true` |

---

## 2. 激活链条现状（含量化）

### 2.1 现状链路（实测定性）

```
                         ┌──────────── 绕过 MoE 的证据写入（占绝对多数）────────────┐
发言/宣称 → Bridge.say → pipeline.absorb ──┬→ AI.addEvent（R 规则链 45 处直接调用）    │
                                          └→ AI.addUniversal                          │
公开批次   → revealPublic → AI.onReveal → MoE.emit('reveal') ─┐                       │
④ 维修暴露 → AI.onExpose(repair) → MoE.emit('④') ────────────┤ MoE.route(12 专家 gate)
⑤ 破坏者暴露→ AI.onExpose(destroy)→ MoE.emit('⑤') ───────────┘   → arbitrate → write
步骤9      → perceive.reason → MoE.runChannelsAll（38 条门禁，绕过路由/仲裁直写）
```

### 2.2 三个结构性断点

| # | 断点 | 证据 |
|---|---|---|
| **B1** | **MoE 只挂 3 个事件**。`MoE.emit` 全仓仅 2 个调用点：`perceive.js:209`（④/⑤）、`:308`（reveal）。`HARD_ROUTE`/`RELEVANCE` 里登记的 **`⑥`/`⑩`/`①`/`pm` 四个事件运行时不可达**——总表里 `on:'⑥'` 有 **20 条**、`pm` 8 条，永远不会经路由触发 | `steps.js:616/661` 是 `onExpose` 仅有的两个调用方 |
| **B2** | **专家没有执行体**。`registry.EXPERTS` 13 条只有 `{name, channels, constant}`，无 `run`；「专家」实际只是 Claim 上的 `expert` 标签。E7/E9/E10/E13 四个标签**无任何打标者**（`moe.js:155` 的 E13 直通分支因此不可达） | 打标点仅 9 处：perceive 6 处 + steps 1 处 + channels.run 2 处 |
| **B3** | **三档仲裁与影子层几乎不出力**。仲裁输入 159460 次，多 Claim 率 0.0025，三档结果「同档保留分歧 406 / 差一档 0 / 差两档 0」；影子层 500 局仅 3570 次调用（只在 `decide.js:192-205` 做戏指控分支），采纳率 2.86% | 与台账 A4 结论一致 |
| **B4** | **通道门禁绕过路由/仲裁**。`runChannelsAll` 直接 `addEvent/addUniversal`，不走 `route`/`arbitrate`——这是设计选择（门禁自带幂等与视角档），但意味着 38 条接线通道不享受 MoE 的饱和/折叠机制 | `channels.run.js:440-455` |

### 2.3 激活读数（500 局）

| 指标 | 读数 | 说明 |
|---|---|---|
| `chan_wired` | **38 / 506（7.5%）** | 详见 §3 |
| `chan_fired` | **23 / 38** | 15 条注册未触发（见下） |
| 求值 / 命中 | 420,470 / 13,111（**3.1%**） | 门禁每夜按观察者逐条求值 |
| `expert_active` | **9 / 13**：E1 E2 E3 E4 E5 E6 E8 E11 E12 | E4/E12 是「打标者存在但通道 0 条」，E7/E9/E10/E13 从未激活 |
| 路由重合度 | 0.93（平均激活 8.6/12 专家） | **由 reveal 事件独大贡献**（6.6M 次评估），不代表路由分化 |
| 三档仲裁 | 同档 406 · 差一档 0 · 差两档 0 | 仲裁几乎不产生作用 |
| 影子层 | calls 3570 · 采纳率 2.86% · p50 −0.0128 | 单一消费点 |
| 证据结构 | expert_marked 345,840 / 662,323（52%）· fold_rate 0.638 | |
| 通道分叉度 / 证据层分叉度 | 0.236（55 src）/ 0.3217 | |
| 硬源锁定 | 2.43 条/局（私有 0.56 / 小组 1.05 / 全场 0.82） | |

### 2.4 15 条「注册但从未命中」的通道（500 局）

| 通道 | 档 | 未命中原因（按判据） |
|---|---|---|
| N98 / N11 / N12 / N18 / N19 / N22 | C±/C | 破坏算术族的高档位分支：要求当夜净破坏量 >3.0~9.0，而 500 局停摆仅 3.0 达 76 局、6.0 仅 7 局、9.0 为 0 —— **事件本身稀有** |
| N09 / N17 | C/C+ | 同上（>1.5s 与「1 只异形+外星人同夜破坏」的组合） |
| A02 | A− | 匿名预告：生产端只产指名预告（A01），**A02 语义无生产者**（台账已知） |
| A08 | A− | 对跳碰撞：需两人宣称同一独占职业，500 局 0 次 |
| N318 | C+ | 转职探测：需神探查验后目标又转职且再发布，极稀有 |
| N141 / N149 | A−/B | 假标记判定：需医生记忆 + 逾期未死 + ⑫=0 的三方同时成立 |
| N188 | A− | 验票官死后冒称：需验票官已出局且有人宣称验票官 |
| N405 | C | 存续型抵挡指纹：需同一目标被挡 ≥3 个非连续夜 |

> 这 15 条不是「写错」，是**判据天然稀有**或**生产端缺失**（A02）。但 500 局 0 命中意味着它们当前对行为零贡献。

---

## 3. 推理库利用率（506 条）

### 3.1 漏斗

```
506 条（v20 全量 −P11 +第22章）
 └─ 38 条接线（7.5%）        ← dormant:false 仅 6 条，字段已失真
     └─ 23 条在 500 局内触发过（4.5%）
         └─ 15 条贡献了 97% 的命中（N04 一条 26,186 次 = 80%）
```

### 3.2 按专家看（资产 → 接线 → 触发）

| 专家 | 名称 | 资产 | 接线 | 触发 | 状态 |
|---|---|---|---|---|---|
| E1 | 破坏分析 | 130 | 20 | 18 | **高度集中**：20 条里 17 条是「破坏算术族」（净破坏量区间→参与者构成），全部挂在步骤 9 的净破坏量差值上 |
| E2 | 感染分析 | 41 | 4 | 4 | 医生标记记忆族（N139/N141/N149）+ N407 |
| E3 | 票型分析 | 50 | 3 | 3 | N182/N188/N216 |
| E5 | 维修分析 | **2** | 1 | 1 | N401（批 3.5 新增）；A07/A10/A18 等维修类通道**不在 E5 名下** |
| E6 | 死亡分析 | **1** | 1 | 1 | N406（批 3.5 新增）；**死亡公告模式类通道一条都没有归 E6** |
| E7 | 承诺追踪 | 21 | 3 | 1 | A01 触发 167 次；**E7 无打标者**（A01/A02 的标签实为 E7？——是，channels.run 显式传 `impl.expert`）|
| E8 | 身份推断 | 38 | **0** | 0 | **38 条全 dormant** |
| E9 | 一致性校验 | 25（Z 档） | 0 | 0 | 预期（Z 是生成前拦截的负样本） |
| E10 | 汇聚层 | 81 | **0** | 0 | C/F 档群体指纹，全部未接线 |
| E11 | 社会网络 | 43 | 6 | 6 | D03/N306/N318/N403~N405 |
| E13 | 自我镜像 | 74 | **0** | 0 | N221~N262（发言暴露自检）+ N324~N335（七矿区影子）——**它们的设计载体就是影子层，而影子层只有 1 个消费点** |
| **E4** | **查验分析** | **0** | — | — | **总表里没有任何通道归 E4**（A01/A02/A08 归了 E7） |
| **E12** | **元认知** | **0** | — | — | 同上（性格观测/噪声自检无通道资产） |

### 3.3 资产与实现的语义错位（不影响行为，但与总表口径不一致）

| # | 错位 | 说明 |
|---|---|---|
| 1 | **`on:'⑤'` 的 122 条资产，实际触发条件不是「⑤ 公告」** | E1 的破坏算术族挂在 `runChannelsAll` 每夜步骤 9 用「净破坏量差值」求值（`_t10Night`），与⑤公告（有破坏即发，500 局 238 次）并不同源——⑤ emit 500 局只发 7 次（破坏者暴露） |
| 2 | **`dormant` 字段与事实相反** | 500 条标 `dormant:true`，其中 32 条实际已接线 |
| 3 | **E4/E12 空席位** | 专家表有名字、总表 0 条资产 |
| 4 | **⑥ 死亡公告类 20 条资产无专属激活路径** | `⑥`/`⑩`/`①`/`pm` 四个事件不可达（B1），这些通道即使接线也挂不上 |

---

## 4. 语言库利用率

### 4.1 Claim kind 全谱系（22 种白名单 × 4 套枚举）

生产端只产 **12** 种；`defend / rally / split / quote / vote / bind / pass / destroy / cure / rescue` **10 种没有 AI 生成端**（quote/vote/bind/pass 有 NLP 识别 + pipeline 消费，靠玩家说出；destroy/cure/rescue 连识别都有）。另有 `reveal` 出现在 `WEIGHT` 与 `renderer` 里、被 `tactics.js` 的 N397 判据引用，但**既不在 `KINDS` 白名单里、也无生产者**。

| kind | 生成（AI/玩家） | NLP 识别 | renderer | pipeline 消费 | 备注 |
|---|---|---|---|---|---|
| accuse | ✅ 5 处 | ✅ 三档 | ✅ | ✅ onAccuse | 最主干 |
| ask | ✅ 2 处 | ✅ | ✅ | ✅（经 dispatchAsk） | |
| claimRole | ✅ tactics N357/N382 | ✅ | ✅ | ✅ onClaim | |
| lock | ✅ 2 处 | ✅（含否定式） | ✅ | ✅ lockHuman/lockEnemy | |
| exclusion | ✅ 1 处 | ✅ | ✅ | ✅ RULE.exclusion | |
| deny | ✅ 1 处 | ✅ 三式 | ✅ | ✅ denyLie | |
| promise | ✅ 2 处（strong/weak） | ✅ | ✅ | ✅ settlePromises | **mid 档无生产者**（全仓无 `tier:'mid'`） |
| abstain | ✅ SAFE N337 | ✅ | ✅ | ✅ D03 对账 | |
| suppress/repair/brew | ✅ speakable 3 类 | ✅ EXP | ✅ | ✅ expClaim | |
| infection | ✅ tactics N360 | ✅ EXP | ✅ | ✅ expClaim | |
| **quote** | ❌ | ✅ | ✅ | ✅ onQuote | 仅玩家可产 |
| **vote** | ❌ | ✅ | ✅ | ✅ R64 对账 | 仅玩家可产 |
| **bind** | ❌ | ✅ | ✅ | ✅ roleBindClaims | 消费后**无人再读** `roleBindClaims` |
| **defend / rally / pass / split** | ❌ | defend/rally/pass ✅ · split ❌（只产 intent） | ✅ | 仅 recordEvidence | **入台账后零消费** |
| **destroy / cure / rescue** | ❌ | ✅ EXP | ✅ | ✅ expClaim | 语言库有、AI 不说 |
| **reveal** | ❌ 无 | ❌ | ✅ | ❌ | **不在 KINDS 白名单**，N397 判据却引用它 |

### 4.2 可说材料（speakable）只有 3/13

`candidatesFor` 仅 3 类：抑制体验 / 维修体验 / 制药进度。**13 个信息源不在其中**：神探已查验池、船员双查结论、私聊交底、濒死名单（P13）、标记清单（P20）、验票官票源、抗体感知（P15）、保镖受袭感知（B05）、破坏者/维修者暴露——这些的「说出来」全部在 `decide.js:245-270` 内联生成，`speakable.js` 形同虚设。

### 4.3 语料字段消费矩阵（tactics.js）

| 字段 | SAFE(13) | ALIEN/XENO_OPTS(37) | PROTECT_OPTS(3) | 消费者 |
|---|---|---|---|---|
| `say` 文本池 | ✅ | ✅ | —（无） | `decide.js:318` / `pickTactic:205` |
| `act` | ✅ kind | ✅ kind+payload | — | `decide.js:320/287` |
| `when` | — | ✅ | — | `pickTactic:195` |
| `risk` | ❌ | ✅ 权重 | — | `pickTactic:191` |
| `id` | ✅ | ✅（N372 特判 + tacticLog） | 仅测试 | |
| `note` / `label` / `role` | ❌ | ❌ | ❌ | **零消费**（纯文档） |
| `SAFE_LIMIT` / `ALIEN` / `XENO` / `ALIEN_UNWIRED` / `PROTECT_FLOOR_ID` | — | — | — | **导出面零引用** |

> N414~N416 的实际权重在 `Tiers.PROTECT`，`PROTECT_OPTS` 三条只是**说明书式资产**（运行时零消费）——这不是 bug（文档 5.4 本就要求「不进估值层」），但要意识到「改 PROTECT_OPTS 不会改行为」。

### 4.4 语言库在三个规则环节对 AI 完全闲置

| 环节 | 规则 | 现状 | 影响 |
|---|---|---|---|
| **遗言** | 2.6.1：出局者于下一场讨论前发表，一次性，可声称任何内容 | `steps.js` `D-will`/`M-will` 的 `req` 带 `p.isHuman` 过滤 ⇒ **AI 出局者不发遗言**（且步骤按「无决策者」整步跳过） | 遗言是出局者**唯一的信息爆发点**（可交代查验结论/私聊所得）；AI 侧完全闲置，且全 AI 局连该步骤都不发生 |
| **验票官专属发言** | 4.9：30 秒专属发言，全体禁言 | `M-speech` 的 `req` 同样带 `isHuman` ⇒ **AI 验票官不做专属发言** | 验票官的核心价值（把票源图变成公开指控）AI 用不出来 |
| **身份备注** | 2.7：本人可见的记事本 | `p.notes` 字段存在但**零消费**；规则明文「异形可逐夜比对标记清单并记入身份备注」 | AI 没有记事本等价物（医生有 `markEverSeen`，其他角色没有） |

---

## 5. 死代码 / 零引用清单（导出面）

### 5.1 确认死代码（全仓零读取）

| 项 | 位置 | 备注 |
|---|---|---|
| `AI.threatOf`（= suspOf 别名） | `ai.js:49` | **同文件注释（L55-57）声称已删除** —— 注释与代码不一致 |
| `AI.knownFaction`（经 knownOf） | `ai.js` | 别名链三层均无使用点 |
| `AI.project / hostileOf / universalOf / ensureE / chanFor` | `belief.js` | 仅文件内自用 |
| `IR.validate` | `ir.js:95` | **kind 白名单因此不生效** |
| `IR.KINDS / WEIGHT / nextId / fromSig`（外部） | `ir.js` | 仅内部使用 |
| `Channels.on / of` | `channels.js` | 查询 API 无人用 |
| `Tactics.SAFE_LIMIT / ALIEN / XENO / ALIEN_UNWIRED / PROTECT_FLOOR_ID` | `tactics.js` | 审计清单形态，无读取点 |
| `MoE.route / arbitrate / EXPERTS / HARD_ROUTE / RELEVANCE / RANK`（外部） | `moe.js` | 仅 emit 内部使用 |
| `moe.js:155` 的 `c.expert==='E13'` 分支 | `moe.js` | 不可达（无 E13 打标者） |
| `channels.run.js:16` 导入的 `GATES/BASE_GATE/THETA_BIAS` | `channels.run.js` | 导入后零使用 |
| `Tiers.RULE.voteQuote` | `tiers.js:198` | **唯一死规则**（N219 票型转述预留，无生产者） |
| `Tiers.BASE_DANGER` | `tiers.js` | **唯一死常量**（2026-09-10 曾标定后回退） |
| `perceive.js:425` 的 `phaseTag`→`PHASES` 链 | — | 有读（decide.js:719/769），非死代码 |

### 5.2 失效的加载清单（会崩）

| 入口 | 问题 |
|---|---|
| `tools/verify-heat.js:10-13` | 缺 `infer/registry`、`infer/channels.run`、`infer/tiers`、`ai/*` 分层、`engine/*`、`corpus/channels.data` |
| `server/server.js:18-21` | 同上（联机实验入口） |

两者均未按 `tools/load-order.cjs`（唯一真源）更新，按当前源码会因 `global.MoERegistry` / `global.SKChannelsData` / `global.AIUtil` 缺失而抛错。

---

## 6. 对照规则 v4.1 的覆盖缺口（按价值排序）

> 口径：规则给玩家的「合法推理素材」是否在推理库里有对应通道、且是否已通电。

### P0（信息量大、资产已在库、只差接线）

| 规则 | 推理点 | 库内资产 | 现状 |
|---|---|---|---|
| **2.2 私聊配对公告 ①** | 每夜公开配对图 = 最强社会网络证据（证群体，普适层最佳素材） | E11：D01/D11/D17/N270/C25/C50 等 | **0 接线**；且 `①` 事件在 HARD_ROUTE 里不可达（B1） |
| **E8 身份推断 38 条** | 对跳/宣称/揭示交叉 | E8 全部 | **0 接线**（专家席位空转） |
| **E13 影子层 74 条** | 「我说这句会不会暴露」的自检（N221~N262）+ 七矿区（N324~N335） | E13 全部 | **0 接线**；影子层仅 1 个消费点（3570 次/500 局） |
| **E10 汇聚层 81 条** | C/F 档群体指纹（证群体不证个体） | E10 全部 | **0 接线** |
| **⑥ 死亡公告模式 20 条** | 死因组合（一夜 2 死、异形 vs 外星人出刀区分） | `on:'⑥'` 20 条 | **0 接线**，且 `⑥` 事件不可达 |
| **E3 票型 50 条** | ⑨/⑩/票源/弃票对账 | E3 50 条 | 3 接线 |

### P1（规则机制明确，但「生产端」缺失——接了也没有输入）

| 规则 | 缺的生产端 | 说明 |
|---|---|---|
| 4.5 P15 抗体感知 | 已落盘（`antibodyFired`）→ N407 已接 | ✅ 完成 |
| 4.6 P13 濒死名单 | 已落盘（`dyingSeen`）→ N406 已接 | ✅ 完成 |
| **4.8 B05 保镖受袭感知** | 引擎 `applyLethal` 已 `priv()` 文本，但**未结构化落盘**（无 `p.ambushLog` 之类） | 受袭类型三分（异形出刀/外星人出刀/枪击）是攻击来源的强私有证据，B05 只标 D+ 且未接线 |
| **5.3 感染层序反馈** | `applyInfection` 对**施加方无任何结构化反馈**（感染被抗体/保镖/巡逻拦截时异形无从得知）——规则 3.3「感染的结果无反馈（通则）」明文如此，**这是规则设计，不是缺陷**；但「异形次夜比对标记清单」的推理（3.3④）在 AI 侧无等价物（异形没有标记清单记忆字段） | AI 缺「施加方记忆」 |
| **6.1 沉默指纹** | 被沉默者次夜「零行动」与放弃行动外观一致（规则明文无痕迹）；观察类通道库内没有 | 属规则口径，非缺陷 |
| 3.3④ 异形标记清单记忆 | 同上（施加方视野，可分辨真伪） | AI 无该记忆字段 |
| **2.7 身份备注** | `p.notes` 零消费 | AI 无记事本 |

### P2（语言库侧，成本最低收益最高）

| 环节 | 缺口 | 说明 |
|---|---|---|
| **遗言** | AI 不发遗言 | `D-will`/`M-will` 加 AI 分支即可（`case 'will'` 已有现成 IR 词表：claimRole/lock/exclusion…） |
| **验票官专属发言** | AI 验票官不做专属发言 | 同上（票源图在 `g.voteHistory`，AI 可直接读） |
| **10 种无生产者的 kind** | defend/rally/split/quote/vote/bind/pass/destroy/cure/rescue | 其中 `defend`（自辩）/`rally`（集火）/`split`（分票）是发言策略的自然组成；`bind` 有消费但消费结果（`roleBindClaims`）无人再读 |
| **`promise` 的 mid 档** | 全仓无 `tier:'mid'` 生产者 | `Tiers.PROMISE` 三档只用了两档 |
| **`reveal` kind** | 不在 `KINDS` 白名单、无生产者，但 N397 判据引用它 | 要么补进 KINDS 并给生产者，要么把 N397 判据改掉 |

### P3（数据卫生，随手可修）

| 项 | 说明 |
|---|---|
| `dormant` 字段失真 | 500 条标 true，其中 32 条已接线 —— 建议以 `MoE.mountedIds()` 为准自动生成，或批量改齐 |
| 4 套 kind 枚举不一致 | `KINDS`(22) / `WEIGHT`(23) / `renderer`(23) / `pipeline`(20)；且 `IR.validate` 无人调用 |
| E4/E12 空席位 | 要么给资产，要么从专家表去掉（否则 `expert_active 9/13` 永远到不了 11） |
| `dormant:false` 只标了 6 条 | 批 3.5 加的 6 条标了，批 3 之前的 32 条没标 |
| `AI.threatOf` | 注释说删了、代码还在 |

---

## 7. 建议的推进顺序（只列方向，未改动任何文件）

| 优先级 | 事项 | 理由 |
|---|---|---|
| **P0-a** | **修 B1：把 MoE.emit 接到 ⑥/⑩/①**（`revealPublic` 已统一收口，① 需在步骤 0 结算后 emit） | 一次改动解锁 ⑥ 20 条 + ⑩ + ① 配对图（E11 的核心资产）的路由与仲裁；这是「接线 38→416」的前置，否则接了也挂不上 |
| **P0-b** | **补 E4/E12 的席位资产或裁撤** | 专家表 13 席有 2 席空、4 席无打标者；「13 专家」的架构叙事与实现不符 |
| **P1** | **E8（38 条）与 E13（74 条）分批接线**：E8 走单体层（对跳/宣称交叉，判据可机械评估）；E13 走影子层（先扩影子层消费点，再接） | 两族判据在总表里最完整、且都已有 `expert` 归组 |
| **P1** | **E10（81 条 C/F 档）批量走普适层** | 普适层已有饱和机制（cap 25/k 60），是「证群体不证个体」的现成容器 |
| **P2** | **遗言 + 验票官专属发言的 AI 分支** | 语言库已有全部 kind 与词表，纯增量；规则 2.6.1/4.9 明文支持 |
| **P2** | **`defend`/`rally`/`split` 的 AI 生成端** | 语言库有识别+消费，只缺生成；让 AI 会自辩、会集火、会分票 |
| **P3** | 数据卫生：dormant 字段、kind 枚举统一、`AI.threatOf`、两个失效工具的加载清单 | 一次性清理，行为中性 |

---

## 8. 本报告的核对方式（可复现）

```bash
cd "C:/Users/ASUS/Desktop/太空杀游戏-完整版"
node tools/test-fix-v26.cjs        # 100/100
node tools/fingerprint.cjs 200 1   # 6c04657a…
node tools/mc.cjs 500 1            # 本报告全部读数的来源
```

静态普查口径：`js/` 29 文件 + `tools/` 15 + `server/` + `index.html` 全文通读；
资产对账用三份 docx 的编号正则抽取（`N\d{2,3}` / `[ABCDFPZ]\d{2}`）与 `channels.data.js` 的 506 条逐一比对。
